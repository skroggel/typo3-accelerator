plugin.tx_accelerator {

    settings {
        cdn {
            enable = 1
            baseDomain = rkw-kompetenzzentrum.rkw.local
        }
	}
}

// needed because of BE-Mode with test
module.tx_accelerator < plugin.tx_accelerator
